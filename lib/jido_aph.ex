defmodule JidoAph do
  @moduledoc """
  APH-notarized signals for jido agents.

  Stub module — the consumer-facing helpers (`attach_notarization/3`,
  `read_notarization/1`) land with the notarization signal extension
  (PRD-001 T4). Nothing here yet makes any verification claim.
  """
end
